## Packages
framer-motion | Complex animations for success states and page transitions
date-fns | Formatting timestamps and dates
clsx | Conditional class names
tailwind-merge | Merging tailwind classes properly

## Notes
- Tailwind config should extend fonts to include 'Outfit' (Display) and 'DM Sans' (Body)
- Auth hooks are assumed to be in client/src/hooks/use-auth.ts (provided by system)
- Mobile-first design approach is strictly followed
- High contrast colors used for outdoor visibility
